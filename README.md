# gmail-expense-tracker
automates personal finance management. It fetches transaction emails, parses amounts, dates, and merchants using custom regex logic, categorizes them, and syncs everything into Google Sheets with auto-sorting.
